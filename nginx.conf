# ================================================================
#  Payin — Nginx Configuration (Subdomain-based)
#  Copy to: /etc/nginx/sites-available/payment
#  Then:    ln -s /etc/nginx/sites-available/payment /etc/nginx/sites-enabled/
#
#  Replace: yourdomain.com with your actual domain
#
#  DNS Records needed (all A records pointing to your server IP):
#    auth.yourdomain.com      → YOUR_SERVER_IP
#    api.yourdomain.com       → YOUR_SERVER_IP
#    tx.yourdomain.com        → YOUR_SERVER_IP
#    wallet.yourdomain.com    → YOUR_SERVER_IP
#    settle.yourdomain.com    → YOUR_SERVER_IP
#
#  SSL (after DNS is set up):
#    sudo certbot --nginx -d api.yourdomain.com -d auth.yourdomain.com \
#         -d tx.yourdomain.com -d wallet.yourdomain.com -d settle.yourdomain.com
# ================================================================

# ------------------------------------------
#  Auth Service — auth.yourdomain.com
# ------------------------------------------
server {
    listen 80;
    server_name auth.yourdomain.com;
    root /var/www/payment/auth-service/public;

    index index.php;
    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ------------------------------------------
#  Payment Service — api.yourdomain.com (main)
# ------------------------------------------
server {
    listen 80;
    server_name api.yourdomain.com;
    root /var/www/payment/payment-service/public;

    index index.php;
    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ------------------------------------------
#  Transaction Service — tx.yourdomain.com
# ------------------------------------------
server {
    listen 80;
    server_name tx.yourdomain.com;
    root /var/www/payment/transaction-service/public;

    index index.php;
    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ------------------------------------------
#  Wallet Service — wallet.yourdomain.com
# ------------------------------------------
server {
    listen 80;
    server_name wallet.yourdomain.com;
    root /var/www/payment/wallet-service/public;

    index index.php;
    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ------------------------------------------
#  Settlement Service — settle.yourdomain.com
# ------------------------------------------
server {
    listen 80;
    server_name settle.yourdomain.com;
    root /var/www/payment/settlement-service/public;

    index index.php;
    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# ================================================================
#  Redirect bare domain to api subdomain
# ================================================================
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    return 301 https://api.yourdomain.com$request_uri;
}
